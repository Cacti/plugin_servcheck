<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="../../include/vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap-unit.php"
         cacheDirectory=".phpunit.cache"
         colors="true"
         failOnWarning="true"
         failOnNotice="true"
         failOnDeprecation="true"
         beStrictAboutOutputDuringTests="false">
    <php>
        <ini name="error_reporting" value="-1"/>
        <env name="CACTI_TEST_BOOTSTRAP" value="1"/>
    </php>
    <testsuites>
        <testsuite name="servcheck">
            <directory>tests/Unit</directory>
        </testsuite>
    </testsuites>

    <!--
        Pest reads this PHPUnit-compatible configuration through Cacti's
        Composer-managed vendor tree, checked out alongside this plugin by
        the CI workflow (see .github/workflows/plugin-ci-workflow.yml).

        tests/Integration and tests/e2e hold standalone PHP scripts (not
        PHPUnit/Pest specs) that are run directly via the PHP CLI, so they
        are intentionally excluded from this testsuite.
    -->

    <source>
        <include>
            <file>setup.php</file>
            <file>includes/arrays.php</file>
            <file>includes/functions.php</file>
        </include>
    </source>
</phpunit>
